# Cloudflare Pages — tells the dashboard where the built site lives
name = "big-day-studios"
pages_build_output_dir = "./dist"
compatibility_date = "2024-09-23"

# Baked into production builds (PUBLIC_* also works in Cloudflare dashboard)
[vars]
PUBLIC_CLOUDINARY_CLOUD_NAME = "dtpsk76yw"
PUBLIC_SITE_URL = "https://bigdaystudios.in"
